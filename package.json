{
  "name": "reconciler",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend-new && npm install && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "readline": "^1.3.0"
  }
}
